#include <iostream>
#include "stack.h"
using namespace std;

// 封装
// 1、将数据和方法定义到一起
// 2、把想给你看的数据给你，不想给你看的进行封装 通过访问限定符进行限制
// 3、C语言中struct使用来定义结构体,也可以用来定义类
// c++中struct和class的区别？
// 1、默认访问权限不同

int main() {
    // 类实例化出对象
    // Stack s1(10);
    // 类实例化的对象中只存储成员变量不存储成员函数
    // 为什么呢？
    // 因为成员函数是公用的，所有对象都可以使用同一份成员函数，存放在公共代码段之中
    // 成员变量是每个对象独有的
    // 没有成员变量的大小是1,这里不是存数据，而是占位表示对象存在
    A1 a1;
    // cout << sizeof(s1) << endl;
    cout << sizeof(a1) << endl;
    a1.PrintDate();
    // 内存对齐介绍
    // 内存对齐是为了提高访问效率
    // 1、基本数据类型的对齐
    // 2、结构体的对齐
    // 3、类的对齐
    // A1* pa1 = NULL;
    A1 a2;
    // 直接去公共代码段中找PrintDate函数
    // pa1->PrintDate(); // 这里通过空指针调用成员函数,这里程序崩溃
    // pa1->PrintNoDate(); // 这里没有调用那个空指针,这里程序不会崩溃






    return 0;
    /* 硬件存储体系
    ### ? 一、存储体系的层次结构（从快到慢，从小到大）

    | 层级 | 存储类型 | 位置 | 特点 | 速度 | 容量 | 挥发性 |
    |------|-----------|------|------|------|------|--------|
    | 1?? | **寄存器（Register）** | CPU内部 | 存储指令和数据的临时结果 | 极快 | 极小（字节级） | 是 |
    | 2?? | **高速缓存（Cache）** | CPU旁边 | 分为 L1、L2、L3，减少主存访问延迟 | 很快 | 小（KB~MB） | 是 |
    | 3?? | **主存（RAM）** | 主板 | 程序运行时的数据存储区 | 快 | 中等（GB级） | 是 |
    | 4?? | **固态硬盘/机械硬盘（SSD/HDD）** | 外部存储设备 | 持久化存储系统和数据文件 | 慢 | 大（TB级） | 否 |
    | 5?? | **外部存储/云存储** | 网络或可移动设备 | 远程访问或便携式存储 | 更慢 | 更大（TB~PB） | 否 |


    ## ? 补充说明

    - **寄存器**：CPU直接操作的存储单元，如程序计数器、累加器等。
    - **Cache**：用于缓解 CPU 与主存之间的速度差异，通常由硬件自动管理。
    - **RAM**：运行程序的主要工作区，断电即失。
    - **SSD/HDD**：用于长期保存数据，SSD 速度远快于 HDD。
    - **外部/云存储**：如 U盘、移动硬盘、NAS、云盘等，适合备份和共享。
    */
}

// #include <iostream>
// #include <string>
// using namespace std;

// // 计算将括号序列变为平衡所需的最少交换次数
// int minSwapsToBalance(const string& s) {
//     int balance = 0;
//     int swaps = 0;

//     for (char c : s) {
//         if (c == '(') {
//             balance++;
//         } else { // c == ')'
//             balance--;
//             if (balance < 0) {
//                 swaps++;
//                 balance = 1; // 假装交换一个左括号过来
//             }
//         }
//     }

//     return swaps;
// }

// int main() {
//     int T;
//     cin >> T; // 输入测试组数
//     while (T--) {
//         string s;
//         cin >> s;
//         cout << minSwapsToBalance(s) << endl;
//     }
//     return 0;
// }
